import { createMarkdownOptions } from "./Options";
/**
 * Create Markdown processor
 * @param options
 */
export declare const createMarkdown: (options?: createMarkdownOptions) => (markdown: string) => string;
