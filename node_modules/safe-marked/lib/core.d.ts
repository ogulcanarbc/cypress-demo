export declare type createCoreProcessorOptions = {
    markdownToHTML: (markdown: string) => string;
    sanitizer: (html: string) => string;
};
/**
 * Create Core processor
 * @param options
 */
export declare const createCoreProcessor: ({ markdownToHTML, sanitizer }: createCoreProcessorOptions) => (markdown: string) => string;
